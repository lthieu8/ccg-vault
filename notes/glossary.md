# Glossary

One definition per domain term. If two specs use a word differently, that is a bug in the specs —
fix it here first, then in both.

Keep definitions short enough that a new developer reads them. A definition nobody reads is worse
than none, because it looks like the term was agreed.

## Template

### <Term>
<One or two sentences. What it is, and the boundary that distinguishes it from the nearest
similar term.>
*Used in:* [[02-specs/example]]
